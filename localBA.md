这是一个典型的多视角几何与局部局部束调整（Bundle Adjustment）问题，但在实际机器人工程中（如你的割草机、泳池机器人系统），由于车体（body）只有 $x, y, \theta$ 约束，这意味着系统存在高度退化的自由度，必须将传统的多维空间几何与机器人的平面运动约束进行紧耦合。
这里有 3 个相机（记为 $C_1, C_2, C_3$），对应的车体位姿为 $B_1, B_2, B_3$。已知 $B_1, B_2$ 的平面位姿（不完美）和外参 $T_B^C$，目标是恢复 $C_3$（及 $B_3$）的绝对物理位姿。
以下是完整的求解步骤、数学管道以及需要考虑的各种退化与边界情况：
🛠️ 第一阶段：初始化与数据准备
由于 $B_1, B_2$ 存在误差，我们不能盲目相信它们，但它们可以作为极佳的尺度和方向先验。
1. 坐标系转换与位姿初值化
利用已知的 3DoF 车体姿态（$x, y, \theta$）和固定的相机外参 $T_B^C = \begin{bmatrix} R_B^C & t_B^C \\ 0 & 1 \end{bmatrix}$，计算 $C_1$ 和 $C_2$ 在世界坐标系下的 6DoF 初始位姿：

$$T_{W}^{C_i} = T_{W}^{B_i} \cdot (T_{B}^C)^{-1}, \quad (i = 1, 2)$$
此时，$C_1$ 和 $C_2$ 之间已经具备了物理绝对尺度（来自 $B_1, B_2$ 之间的相对位移）。
2. 特征匹配与三维空间点重构（Triangulation）
跨三帧匹配：利用提取的特征向量，通过匹配算法（如你熟悉的 LightGlue），找出在三张图像上共同观测到的特征点对：$\mathcal{M} = \{x_1^k \leftrightarrow x_2^k \leftrightarrow x_3^k\}, \quad (k=1 \dots N)$。
三角化 3D 点（关键步）：由于 $C_1$ 和 $C_2$ 的绝对位姿已知（带噪），利用点对 $\{x_1^k \leftrightarrow x_2^k\}$ 以及对应的投影矩阵 $P_1, P_2$，通过三角化（Triangulation，如直接线性变换 DLT 法）解算出空间点在世界坐标系下的三维坐标 $X_W^k = [X, Y, Z, 1]^T$。
📐 第二阶段：求解第 3 个相机的初值 (PnP)
现在你手中拥有了一批带有绝对物理尺度的世界坐标系 3D 点 $X_W^k$，以及它们在第 3 个相机上的 2D 投影点 $x_3^k$。这是一个标准的 PnP（Perspective-n-Point） 问题。
3. RANSAC + PnP 粗解位姿
排除误匹配：使用 cv2.solvePnPRansac，输入 3D 点 $X_W^k$ 和 2D 点 $x_3^k$，以及相机 3 的内参。
输出：这一步可以过滤掉跨帧匹配中的外点（Outliers），并求解出第 3 个相机相对于世界的粗略位姿 $T_W^{C_3\_init}$。
⛓️ 第三阶段：联合图优化后端 (Bundle Adjustment)
这是解决“前两个相机位姿不准”以及“body只有 $x,y,\theta$”的核心步骤。
我们需要建立一个非线性最小二乘优化（因子图），将 $B_1, B_2, B_3$ 的运动约束与视觉重投影误差融合。
4. 构建状态量 (States)
优化变量包括：
三个车体的位姿：$\mathcal{X}_{body} = \{B_1, B_2, B_3\}$，其中 $B_3 = [x_3, y_3, \theta_3]^T$（强行约束在平面）。
所有被三角化出来的 3D 空间点：$\mathcal{X}_{points} = \{X_W^1, X_W^2 \dots X_W^N\}$。
5. 构建损失函数 (Cost Function)
优化目标是最小化以下三个因子的加权总和：

$$\min_{\mathcal{X}_{body}, \mathcal{X}_{points}} \left( E_{visual} + E_{prior\_12} + E_{motion\_3} \right)$$
视觉重投影残差 ($E_{visual}$)：
对于每一个相机 $i \in \{1, 2, 3\}$ 看到的空间点 $k$：
$$r_{visual} = \left\| x_i^k - \pi \left( T_B^C \cdot (T_W^{B_i})^{-1} \cdot X_W^k \right) \right\|_{\Sigma_v}^2$$
注：$\pi$ 为相机投影函数，$\Sigma_v$ 为像素噪声协方差（通常设为 1-2 像素）。
前两个相机的先验残差 ($E_{prior\_12}$)：
由于已知 $B_1, B_2$ 不那么准，我们不能把它们设为固定死（Anchor），而是给它们施加一个高斯先验约束。让它们在优化中可以微调（微调范围取决于你传感器给出的置信度）：
$$r_{prior} = \left\| B_1 - \hat{B}_1 \right\|_{\Sigma_{B1}}^2 + \left\| B_2 - \hat{B}_2 \right\|_{\Sigma_{B2}}^2$$
车体平面运动约束 ($E_{motion\_3}$)：
这一步至关重要！ 因为车体只有 $x, y, \theta$，如果相机 3 仰角或高度算歪了，需要靠这个强约束拉回来。对于 $B_3$，强迫其 $z_3=0, \phi_3=0, \psi_3=0$（高度、滚转、俯仰为 0，或为车体物理底盘的固定常数）。
6. 求解
使用 Ceres-Solver 或 g2o 运行高斯-牛顿（Gauss-Newton）迭代，最终输出最符合物理约束的 $B_3$ 绝对位姿以及优化后的 $B_1, B_2$。
⚠️ 各种必须考虑的退化与特殊情况 (Edge Cases)
在割草机或泳池机器人等实际场景中，以下几种情况会导致上述算法直接崩溃，必须在代码中做分支防御：
情况 A：前两帧运动处于“纯旋转”或“极小视差”
现象：机器人原地打转，或者移动了不到 2 厘米。
后果：第一阶段的三角化会彻底失效（深度无限大或噪声巨大），PnP 随之崩溃。
解法：检查 $B_1$ 和 $B_2$ 之间的相对位移。如果物理距离小于某一阈值（如小于 10 厘米），拒绝进行三角化。改用相机 1 和相机 3（或者 2 和 3）之间有足够大视差的关键帧对来进行初始化。
情况 B：特征点全落在超远景（如天空、远山、远处的围栏）
现象：割草机在空旷地带，相机朝上看。
后果：远景点的视差极小，无法三角化出可靠的绝对深度，导致绝对尺度丢失。
解法：在三角化后，计算 3D 点在相机坐标系下的深度 $Z$。过滤掉所有 $Z > 15 \text{m}$ 或 $Z < 0$ 的点。确保参与 PnP 的点属于中近景（$0.5\text{m} \sim 10\text{m}$）。
情况 C：特征点全部共面（面对一面白墙，或者全在平整的草地上）
现象：特征点全部在水平地面上。
后果：标准的 3D-2D PnP 求解可能会出现多解性（Ambiguity），尤其是相机的俯仰角（Pitch）容易和小车的 $y$ 轴平移产生退化耦合（算法分不清是小车往前挪了，还是相机往下低头了）。
解法：在后端优化中，必须引入车体非完整性约束（Non-holonomic Constraint），即不允许车体发生侧滑（$v_y = 0$），或者强迫特征点的空间分布通过 ANMS 算法在图像上下均匀散开，引入非地面的背景点约束。
情况 D：外参 $T_B^C$ 存在安装误差
现象：产线组装时，相机装歪了 1 度。
后果：由于车体只有 $x, y, \theta$，外参的小误差会导致视觉投影出来的 3D 点在世界坐标系里发生“扭曲”，极大地污染 PnP 的求解。
解法：在非线性优化（BA）中，将外参 $T_B^C$ 也作为待优化变量（Online Calibration）加入状态量，但给定一个很强的边界范围（例如旋转微调 $\pm 1^\circ$ 空间），允许算法自动吸收安装误差。
